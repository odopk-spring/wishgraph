# v0.1.0 public beta release checklist

The release owner checks every item against the exact candidate commit. A public
beta must be marked as a GitHub prerelease.

## Product and policy

- [x] `skills/wishgraph/VERSION` is `0.1.0`.
- [x] PolyForm Noncommercial remains the intended release license.
- [x] README claims match `COMPATIBILITY.md`, `PRIVACY.md`, and known limitations.
- [x] `CHANGELOG.md` has a release date and final notes.
- [ ] Security and commercial contact addresses are monitored.

## Real-host acceptance

Release-owner attestation on 2026-07-21: the host loops were exercised on other
computers and passed. Exact host versions and machine-local logs were not
transferred into this checkout; the public beta proceeds on that explicit
attestation without inventing environment details.

| Environment | Host version | Mode | Fresh install | Full Task loop | Reopen/resume | Result / evidence |
|---|---|---|---|---|---|---|
| Windows + Codex | Not recorded | warn | ✓ | ✓ | ✓ | Passed off-device; release-owner attestation |
| macOS/Linux + Codex | Not recorded | warn | ✓ | ✓ | ✓ | Passed off-device; release-owner attestation |
| macOS/Linux + Claude Code | Not recorded | warn | ✓ | ✓ | ✓ | Passed off-device; release-owner attestation |
| Selected host | Not recorded | enforce | ✓ | ✓ | ✓ | Passed off-device; release-owner attestation |
| Older runtime → v0.1.0 | Automated fixture | preserved | N/A | N/A | ✓ | Safe-upgrade regression suite passed locally |

## Automated evidence

- [ ] Clean worktree and candidate commit reviewed.
- [x] Full unit suite passes locally on the candidate worktree (293 tests, 9 skipped).
- [ ] Ubuntu/macOS Python 3.9 and 3.13 jobs pass.
- [ ] Windows Python 3.13 and PowerShell installation smoke test pass.
- [x] Three-round cold-process benchmark passes locally on Windows 11 / Python 3.13.12.
- [x] Release owner accepted a 250 ms ordinary PreToolUse gate on GitHub-hosted
      Windows runners; Ubuntu/macOS remain at 200 ms.
- [ ] Tagged Bash and PowerShell installers install `v0.1.0`, not `main`.
- [x] Runtime manifest fingerprints match and safe upgrade tests pass locally.
- [ ] Release archives and `SHA256SUMS` are generated by CI.

## Publish and rollback

- [ ] Create annotated tag `v0.1.0` at the validated commit.
- [ ] Confirm the tag workflow passes before publishing the draft release.
- [ ] Mark the GitHub release as **Prerelease**.
- [ ] Attach the generated release archives and `SHA256SUMS`.
- [ ] Test one public installation command after publishing.
- [ ] If installation is broken, leave the immutable tag intact, mark the release
      affected, restore the documented prior tag, and publish a patch version.
